# Public values - these are NOT encrypted
replicaCount: 1

image:
  repository: nginx
  tag: "1.21"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

# Note: Secret values are in values.secret.yaml (encrypted)

