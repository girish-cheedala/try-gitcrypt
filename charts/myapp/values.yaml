# Public values - these are NOT encrypted
replicaCount: 1

image:
  repository: nginx
  tag: "1.21"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

# Note: Secrets are stored in secrets/.env file (encrypted by git-crypt)
# The .env file is read by the Secret template and mounted as volumes

